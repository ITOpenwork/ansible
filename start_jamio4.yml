---

- name: Start Jamio4 service
  hosts: "{{ servers }}"
  gather_facts: false
  tasks:
  - name: Start JamioAdmin4 & Jamio4
    win_service:
      name: JamioAdmin4
      state: started
      name: Jamio4
      state: started
